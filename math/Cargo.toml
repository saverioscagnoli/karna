[package]
name = "math"
version = "0.1.0"
edition = "2024"

[dependencies]
utils.workspace = true
winit.workspace = true
num-traits = "0.2.19"
serde = { version = "1", optional = true }

[features]
serde = ["dep:serde"]
